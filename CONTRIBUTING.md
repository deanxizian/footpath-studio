# 参与开发

应用首次导入及后续改动通过功能分支和 Pull Request 合入 `main`。仓库的初始化提交只建立许可证和忽略规则。

1. 说明具体问题和改动后的行为。
2. 运行 `pnpm format:check`、`pnpm test`、`pnpm build`；界面变更检查桌面和手机布局。
3. 附相关验证结果和截图，推荐使用合成数据。
4. 处理审阅意见，通过必需的 CI 后合并。

分支保护要求 PR、最新分支通过 `test-and-build`、解决审阅对话，禁止强制推送和删除 `main`。当前个人维护模式不要求他人批准票；这不等同于独立维护者已审阅。审阅者应在 PR 中记录实际检查范围及发现的问题。

CI 使用合成测试数据，不读取 Stryd 会话或仓库密钥。涉及抓取逻辑时，还需安装 `scripts/stryd/requirements.txt` 并运行 `python -m unittest discover -s tests/stryd -v`。算法修改应说明样本、筛选和缺失值口径，不把缺失值变为零。

不要提交凭据、浏览器状态、本地数据或构建产物。维护者已授权的公开历史数据仅通过专用 Release 附件分发，更新来源、摘要及快照日期需另开 PR；未经数据所有者许可，不要在 PR、截图或日志中公开他人的活动。
